fastapi==0.136.3
uvicorn[standard]==0.49.0
beanie==2.1.0
pydantic-settings==2.14.1
email-validator==2.3.0
pyjwt==2.13.0
argon2-cffi==25.1.0
httpx==0.28.1
aiosmtplib==5.1.1
redis==5.2.1
asyncpg==0.30.0
jinja2==3.1.6
python-multipart==0.0.20
# Pure-Python git (no native `git` binary). Backs the Mods Hub git store + the
# authenticated smart-HTTP server (`git clone/pull/push` over /git/mods/*).
dulwich==0.22.8
cryptography==46.0.3
tzdata==2025.2
beautifulsoup4==4.12.3
pillow==11.3.0
# Blueprint→image voxel rasterizer (app/trove/render) - CPU-only, no GPU.
numpy==2.4.6
# Self-hosted OCR engine for the character-stat extractor (/v1/ocr/character).
# RapidOCR (PaddleOCR models) - accurate on stylized game fonts + multilingual,
# pip-only (no system OCR binary, no torch). The `rapidocr` package supports
# Python 3.13 (the legacy `rapidocr-onnxruntime` caps at <3.13) but does NOT pull
# an inference engine, so onnxruntime is pinned explicitly. Needs libgl1/
# libglib2.0-0 (Dockerfile); models are baked into the image at build time.
rapidocr==3.8.4
onnxruntime==1.26.0
discord.py==2.4.0
# discord.py imports the stdlib `audioop` module at import time (for voice, which
# we don't use). Python 3.13 REMOVED audioop from the stdlib, so on the 3.13 bot
# image `import discord` raises ModuleNotFoundError and the gateway bot crash-loops.
# audioop-lts is the maintained backport that restores it on 3.13+.
audioop-lts==0.2.1; python_version >= "3.13"
