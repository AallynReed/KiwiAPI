# pvpplayercounterui.swf
> A PvP HUD element that displays a player count or score counter during PvP matches in Trove. The SWF's scripts/ directory is entirely empty — no ActionScript classes of any kind were decompiled from it.

**Document/main class:** None (scripts/ directory is empty)
**SWF-specific classes:** 0

---

## Notes

The `scripts/` folder for this SWF contains no `.as` files whatsoever — not even shared framework classes. This indicates that the SWF is either:

1. A purely visual/asset SWF with no ActionScript logic (all behaviour driven by the parent shell or timeline-only scripts with no package-level classes), or
2. A very thin wrapper whose only code lives in unnamed timeline frame scripts that JPEXS ffdec did not extract as separate `.as` files.

No `ExternalInterface` calls, translate keys, Iggy runtime dependencies, or data hooks can be documented because no source was available.
